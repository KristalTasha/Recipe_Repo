<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="../home.css" />
    <link rel="stylesheet" href="../searchresults.css" />
    <link rel="stylesheet" href="https://pro.fontawesome.com/releases/v5.10.0/css/all.css"
        integrity="sha384-AYmEC3Yw5cVb3ZcuHtOA93w35dYTsvhLPVnYs9eStHfGJvOvKxVfELGroGkvsg+p" crossorigin="anonymous" />
    <title>Recipe</title>
</head>

<body>


    <header>
        <nav class="navbar">
            <div class="navsection">
                <div class="logoback">
                    <p class="logo">Tasha's Recipes</p>
                </div>

                <ul class="navlist">
                    <a class="navitem" href="/">Home</a>
                    <a class="navitem">About us</a>
                    <a class="navitem">Contact us</a>
                </ul>
            </div>
            <div class="search">
                <input class="searchfield" type="text" placeholder="Search by recipe or name">
                <i class="fa fa-search"></i>
            </div>
        </nav>
    </header>

    <div class="page">

        <div class="searchres-cont">
            <p class="toptext">All Recipes</p>

            <div class="top-recipe-grid">
                <% if(found.length > 0){ %>
                <% found.forEach(card => { %>

                <div class="top-recipe-card">
                    <img class="card-img" src="/uploads/<%=card.recipe_img%>">
                    <div class="card-label">
                        <a href="/recipe/<%=card._id%>">
                            <p class="food"><%=card.recipe_name%></p>
                        </a>
                        <a href="/profile/<%=card._id%>">
                            <p class="card-chef"><%=card.full_name%></p>
                        </a>
                    </div>
                </div>

                <% }) %>
                <% } else { %>
                <p><%= "No cards to display currently" %> </p>
                <% } %>

            </div>

        </div>

    </div>
</body>

</html>